class Turn {
  constructor(guess, card){
    this.guess = guess;
    this.card = card;
  }

  returnGuess(){
// return this.guess
  }

  returnCard() {
// return current card from card class
  }

  evaluateGuess() {
// return if guess is true(correct) or false(incorrect)
  }

  giveFeedback() {
// return either 'incorrect!' or 'correct!
  }
}